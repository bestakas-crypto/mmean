================================================================================
MMEAN — 나중에 할 작업 목록 (after.txt)
작성 규칙: 완료 시 [DONE yyyy-mm-dd] 태그 추가
우선순위: 🔴 높음(버그/결함) / 🟡 중간(기능 개선) / 🔵 낮음(장기/선택)
================================================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔴 결함 / 잠재적 버그
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[B-001] rag_prep 자동 트리거에 step1 누락  [DONE 2026-03-22]
────────────────────────────────────────────────────────────────────────────────
심각도: 높음
원인:
  app_bootstrap.py 의 _run_rag_prep_if_stale()이 step2+4 만 실행.
  step2는 market_features 테이블에 의존하는데,
  market_features 는 step1_compute_features()가 regime_ticks 를 읽어 채워줌.
  market_features가 비어있으면 step2는 항상 0건 → pattern_memory 미갱신.
증상:
  - rag_prep 헬스 패널에서 status=success 인데 pattern_memory=0 유지
  - entry_gate 가 실질적으로 항상 ALLOW
수정 방향:
  _run_rag_prep_if_stale() 에서 step 순서를 (1, 2, 4) 로 변경.
  또는 market_features 행 수를 staleness 판단 기준에 추가.
전제조건: 없음 (즉시 수정 가능)

[B-002] entry_gate_log INSERT 시 connection open/close 반복  [DONE 2026-03-22]
────────────────────────────────────────────────────────────────────────────────
심각도: 낮음 (상태 전이 감지로 빈도 감소 후)
원인:
  engine_runtime.py 에서 REJECT/HALF 전이마다 sqlite3.connect() → commit() → close() 호출.
  상태 전이 시만 실행되므로 빈도는 낮지만, DB 잠금(WAL) 충돌 가능성 잔존.
수정 방향:
  threading.Queue 기반 비동기 DB 쓰기 패턴으로 교체.
  또는 runtime 에 공유 DB 커넥션 보유.
전제조건: 없음 (안정성 개선 목적)

[B-003] _warning_level 임계값 근거 없음
────────────────────────────────────────────────────────────────────────────────
심각도: 중간
원인:
  rag_prep.py 의 _warning_level() 함수:
    danger  : win_rate < 0.25 AND avg_pnl < -0.5
    warning : win_rate < 0.35 AND avg_pnl < -0.2
    caution : win_rate < 0.45
  이 수치는 임의값이며 실데이터 기반 교정 이력이 없음.
  너무 엄격하면 entry_gate 가 과도하게 발동, 너무 느슨하면 사실상 무의미.
수정 방향:
  2~3주 데이터 축적 후 분포 분석 → 백분위 기준으로 재설정.
  POLICY.md 에 임계값 변경 이력 기록 의무화.
전제조건: 실거래/시뮬 2~3주 데이터 필요


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🟡 기능 개선 / 운영 편의
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[A-001] 실제 진입 누적 카운터 (대시보드)
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (기존 sim_trades로 대체 가능)
배경:
  gate 개입 횟수(REJECT/HALF)와 실제 진입 횟수를 나란히 봐야
  "많이 막았는데도 실제 진입은 충분했는지"를 판단할 수 있음.
구현 방향:
  - engine_runtime.py: LONG/SHORT 진입 확정 시 카운터 증가
  - HALF 상태에서 진입한 경우 별도 half_entries 카운트
  - Gate 패널에 게이트 카운터 옆 표시
전제조건: 없음

[A-002] 게이트 효과 분석 + 정책 검증 리포트
────────────────────────────────────────────────────────────────────────────────
우선순위: 높음 (단, 데이터 축적 후) — 【A-003 Ambiguous v2 설계의 전제조건】
배경:
  entry_gate 임계값이 적절한지 감이 아니라 숫자로 확인해야 함.
  단순 "몇 번 발동했는지" 에서 "실제로 손실을 회피했는지" 로 나아가야 함.

  1단계 — 발동 빈도 집계:
    ALLOW/HALF/REJECT 비율, LLM positive → REJECT 횟수,
    warning_level별 빈도, forbidden(MED/HIGH) 차단 횟수

  2단계 — 정책 개입 효율 분석 (더 중요):
    - REJECT 발동 후 실제 진입이 없었고 그 방향이 손실이었는지
    - HALF 발동 후 진입했을 때 일반 진입 대비 손익 비교
    - REJECT가 수익 기회를 불필요하게 버렸는지 (과보수 판단)
    - LLM raw positive + REJECT → 결과가 어땠는지

구현 방향:
  - entry_gate_log + sim_trades 조인 집계 스크립트
  - 날짜별 비교 리포트 (터미널 또는 HTML)
전제조건: entry_gate_log + sim_trades 2주치 이상 필요 → 2026-04월 이후 권장
※ 이 분석 결과가 B-003(임계값 튜닝)과 A-003(Ambiguous v2) 설계의 입력이 됨

[A-003] failure_patterns → Ambiguous proxy v2 (실시간 구조적 충돌 감지)
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (장기 과제)
배경:
  현재 Ambiguous proxy v1 = forbidden 패턴(과거 90일 집계, backward-looking).
  진짜 Ambiguous = 실시간 지표 간 충돌 (베이시스 방향 vs 외국인 vs OI 불일치).
구현 방향:
  - 실시간 feature 벡터에서 지표 간 충돌 스코어 계산
  - entry_gate에 별개 경로로 반영
전제조건: v1 데이터 충분히 쌓인 뒤 비교 실험 필요

[A-004] entry_gate_log DB 조회 API + 대시보드 영구 이력
────────────────────────────────────────────────────────────────────────────────
우선순위: 중간
배경:
  현재 대시보드 발동 이력(flow_gate_log)은 인메모리 — 엔진 재시작 시 초기화.
  DB의 entry_gate_log를 조회해 오늘 이력 전체를 보여줘야 운영에 유용함.
구현 방향:
  - routes/routes_analytics.py 에 /api/gate-log?date=YYYY-MM-DD 엔드포인트
  - 대시보드: 인메모리 이력을 DB 조회로 교체 (페이지 로드 시 오늘 이력 로드)
전제조건: 없음

[A-005] CB 발동 이력 기록
────────────────────────────────────────────────────────────────────────────────
우선순위: 중간
배경:
  CB1(WS단절) ~ CB4(동일방향쿨다운) 차단기 발동 카운터는 있으나
  언제, 어떤 이유로 발동했는지 이력이 없음.
  _cb_reason 문자열이 final_gate_reason 에는 반영되지만 DB에 저장되지 않음.
구현 방향:
  - entry_gate_log 에 cb_reason 컬럼 추가 (ALTER TABLE)
  - CB 발동 시 cb_reason 포함해서 INSERT
  - 또는 별도 cb_log 테이블

[A-006] pattern_memory = 0 시 대시보드 경고 강화
────────────────────────────────────────────────────────────────────────────────
우선순위: 중간
배경:
  RAG Prep 헬스 패널에서 pm_count=0이면
  "entry_gate 비활성" 경고를 명시적으로 표시해야 함.
  현재는 숫자가 빨간색이지만 의미가 직관적으로 드러나지 않음.
구현 방향:
  - pm_count=0 일 때 Gate 패널 상단에 "⚠ pattern_memory 비어있음 — entry_gate 무효" 경고 배너
  - RAG Prep 패널에도 별도 경고 문구

[A-007] rag_prep.bat step1 메뉴 추가
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음
배경:
  현재 rag_prep.bat 메뉴에 step1(market_features 변환) 단독 실행 항목 없음.
  B-001 수정 전까지는 수동으로 step1을 먼저 돌려야 market_features가 채워짐.
구현 방향:
  rag_prep.bat 에 [5] step1 단독 / [6] step1+2+4 전체 메뉴 추가


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔵 정책 / 설계 검토
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[C-001] LLM 타임아웃/오류 시 entry_gate 동작 정책 명확화
────────────────────────────────────────────────────────────────────────────────
배경:
  LLM 호출이 실패(타임아웃, API 오류)하면 weight_adjust=0 으로 처리됨.
  이 경우 entry_gate 계산은 독립적으로 진행되므로 gate는 ALLOW 가능.
  즉, "LLM 실패 + pattern_memory 정상" 조합이면 gate 기준만으로 진입.
  이게 의도된 정책인지 POLICY.md에 명시 필요.
검토 항목:
  - LLM 연속 N회 실패 시 entry_gate를 HALF 이하로 강제하는 정책 필요 여부
  - flow_llm_error 상태일 때 final_gate_reason 에 "llm_error" 포함 여부

[C-002] sim_rag_builder.py 와 rag_prep.py 역할 혼동 방지
────────────────────────────────────────────────────────────────────────────────
배경:
  sim_rag_builder.py : sim.db 기반 (sim_adoptions → pattern_docs)
  rag_prep.py        : mmean.db 기반 (regime_ticks → market_features → pattern_memory)
  두 파이프라인이 독립적으로 동작하며 결과물 테이블도 다름.
  그러나 둘 다 "RAG"라는 이름이 붙어있어 역할이 혼동됨.
검토 항목:
  - POLICY.md 에 두 파이프라인의 역할 구분 명시
  - 각 파이프라인 출력물과 소비자(어디서 읽는지) 매핑 문서화

[C-003] HALF 발동 시 실제 사이즈 축소 end-to-end 검증
────────────────────────────────────────────────────────────────────────────────
배경:
  HALF → _wa_scaled = -0.20 → order_size = base × regime_weight × 0.80 (이론값).
  이게 PositionEngine을 거쳐 sim_engine.on_tick() / live_executor.try_entry() 까지
  실제로 0.80 배율로 전달되는지 코드 추적 검증 필요.
  POLICY.md에 "HALF ≠ 50%" 문구가 있으나 코드 레벨 확인 이력 없음.

[C-004] judgment_events → failure_patterns 데이터 흐름 검증
────────────────────────────────────────────────────────────────────────────────
배경:
  step4_build_failure_patterns() 가 judgment_events 집계.
  judgment_log.open_event() / close_event() / update_llm() 이 호출되고 있으나,
  실제로 judgment_events 에 데이터가 쌓이는지 운영 중 확인 필요.
  비어있으면 failure_patterns 도 영구 비어있어 Ambiguous 게이트 무효.
검증 방법:
  엔진 1~2일 운영 후 SELECT COUNT(*) FROM judgment_events 로 확인.

[C-005] entry_gate 정책 임계값 프로모션 파이프라인
────────────────────────────────────────────────────────────────────────────────
배경:
  현재 _warning_level 임계값은 하드코딩(rag_prep.py).
  향후 A-002 리포트 기반으로 임계값 변경이 필요해질 때,
  변경 → 검증 → 프로모션 파이프라인이 없으면 임의 수정만 가능.
구현 방향:
  config_manager.py 에 warning_level 임계값 파라미터 추가
  (danger_wr, warning_wr, caution_wr, danger_pnl, warning_pnl)
  대시보드 파라미터 설정 패널에서 직접 조정 가능하게.
전제조건: A-002 리포트 데이터 필요


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔵 구조 개선 (장기 / 안정화 후 진행)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[S-001] engine_runtime.py 분해
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (지금 건드리면 리스크가 수익보다 큼 — 안정화 후 진행)
현황:
  core/engine_runtime.py  2,115줄
  engine_loop()           779줄 (단일 함수)
  _llm_call_in_bg()       208줄
  단일 함수가 세션 제어 + WS 수신 + 레짐 계산 + entry_gate +
  LLM 호출 + 주문 분기 + 예외 복구를 동시에 담당.
문제:
  - 변경 한 군데가 다른 5군데를 깨뜨릴 수 있는 높은 결합도
  - 버그 발생 시 어느 계층 문제인지 분리 어려움
  - 테스트 포인트 분리 불가
분해 방향 (최소 단위):
  1. ws_ingest.py          — WS 수신 + investor 데이터 파싱
  2. engine_orchestrator.py — engine_loop 오케스트레이션
  3. llm_dispatch.py       — LLM 호출 / 비동기 bg call 관리
  4. execution_bridge.py   — execute_entry / execute_exit 분기 + 결과 처리
  5. watchdog.py           — session_watchdog_loop / 복구 로직
  6. session_mgr.py        — 세션 감지 / 개장·마감 처리
전제조건:
  - 엔진 2~3개월 운영 후 안정적 동작 확인
  - 주요 실행 경로 통합 테스트 스크립트 작성 선행 권장
  - S-002(flat import 전환) 완료 후 진행 권장
주의:
  지금 시점 진행 시 핵심 루프 동작 보장 어려움.
  데이터 축적 + 안정화 이후 단계적으로 분리할 것.

[S-002] flat import → 명시적 패키지 import 전환
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (path_setup 순서 수정으로 급한 불은 끔 — 장기 정비)
현황:
  engine_runtime.py, app_bootstrap.py 등에서 sys.path 암묵적 의존 혼재.
    from regime_engine import BiasInputs        ← 암묵적
    from kis_tr_catalog import WS_TR_BY_SESSION ← 암묵적
  idx/ 추가 후 신규 flat import 포함:
    engine_runtime.py: from contract_spec import NORMAL_TICK_PT  ← 암묵적
    logs/judgment_log.py: from contract_spec import NORMAL_TICK_PT ← 암묵적
    routes_core.py: from mst_parser import load_contracts ← 암묵적
    (현재는 path_setup.py가 idx/ 등록으로 정상 작동 — 장애 없음)
전환 방향:
    from regime.regime_engine import BiasInputs
    from core.kis_tr_catalog import WS_TR_BY_SESSION
    from core.kis_order_api import get_order_token
    from idx.contract_spec import NORMAL_TICK_PT
    from idx.mst_parser import load_contracts
전제조건:
  - 영향 파일 전수 audit 선행 (engine_runtime, app_bootstrap, sim_opt/*, 등)
  - S-001 분해와 병행하면 효율적


[A-008] HALF 토큰명 / 동작 정의 일원화
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (운영 중 혼란이 누적되면 올림)
배경:
  현재 HALF는 이름이지만 실제 구현은 _wa_scaled = -0.20 (최대 SIZE_DOWN 강제).
  실제 주문 사이즈 = base × regime_weight × 0.80 — "절반"이 아님.
  POLICY.md에 "HALF ≠ 50%" 문구가 있지만 운영이 길어질수록 혼란 재발 가능.
결정해야 할 것 (셋 중 하나):
  1. 현행 유지 + POLICY.md 강조 문구 유지 — 비용 최소
  2. 토큰명 변경: HALF → SIZE_DOWN_MAX (코드·대시보드·문서 전체)
  3. 구현 변경: 실제 50% 수량 절단 로직 (PositionEngine 수정 필요)
권고:
  운영자가 HALF를 보고 실제 오더 사이즈를 잘못 추정하는 일이 반복되면 2번.
  그렇지 않으면 현행 유지.
전제조건: 없음 (의사결정만 필요)

[A-009] success 패턴 LLM 주입 재도입 여부
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음
배경:
  현재 success 패턴을 LLM에 주입하지 않는 건 의도된 정책 (POLICY.md 명시).
  목적이 수익 확대보다 실패 회피이기 때문.
  데이터가 충분히 쌓이면 재검토 가능하나, 아래를 먼저 검증해야 함:
  - success 주입이 확증편향을 키우는지 (LLM이 진입 방향으로 과도하게 기울어지는지)
  - failure 회피 대비 실질적 수익 개선 효과가 있는지
  - 특정 장세(추세장)에서만 제한적으로 써야 하는지
전제조건: A-002 효과 분석 + 3개월 이상 운영 데이터 필요

[W-001] 웹훅 수신 엔드포인트 rate limit
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (클라이언트 수가 늘거나 외부 노출 시 재검토)
배경:
  POST /api/webhook/state 는 현재 rate limit 없음.
  등록된 클라이언트 IP 가 폴링 방식으로 쓰거나, 클라이언트 수가 많아지면
  Flask 메인 스레드에 부하가 걸릴 수 있음.
구현 방향 (규모 커질 때):
  - flask-limiter 로 IP 또는 토큰 단위 제한 (예: 60회/분)
  - 혹은 nginx 리버스 프록시 레벨에서 처리 (애플리케이션 코드 변경 없음)
전제조건: 클라이언트 수 5개 이상 or 외부 인터넷 노출 시

[W-002] 토큰 회전 정책 (token rotation)
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (현재 토큰은 수명 제한 없음)
배경:
  CLIENT_N_TOKEN 은 .env.webhook 에 정적으로 설정되며 만료 개념이 없음.
  토큰이 유출되면 재시작 없이는 무효화 불가.
구현 방향 (운영 규모 커질 때):
  - .env.webhook 에 토큰 갱신 후 재시작으로 즉시 반영 (현행 구조로 충분)
  - 또는 토큰 만료 일시(CLIENT_N_TOKEN_EXPIRES) 필드 추가
  - 또는 DB 기반 토큰 테이블 전환 (재시작 없이 revoke 가능)
현행 운영 수준에서는 재시작으로 토큰 교체 가능 → 즉시 수정 불필요

[W-003] 웹훅 발신 thread pool 전환
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (현 트리거 빈도에서는 신규 thread 비용 무시 가능)
배경:
  send() 는 이벤트(진입/청산/강제청산)마다 클라이언트 수만큼 daemon thread 신규 생성.
  진입/청산은 하루 수 회 수준 → 현행 구조 문제없음.
구현 방향 (클라이언트 수 10개 이상 or 고빈도 이벤트 추가 시):
  concurrent.futures.ThreadPoolExecutor(max_workers=클라이언트 수) 로 교체
  AppRuntime 에 _webhook_pool 필드 보유 → send() 에서 pool.submit() 호출
전제조건: 클라이언트 10개 이상 or 발신 이벤트 유형 대폭 확장 시

[W-004] 조회 fields 화이트리스트 엄격화
────────────────────────────────────────────────────────────────────────────────
우선순위: 낮음 (현재 노출 데이터가 민감하지 않음)
배경:
  POST /api/webhook/state 의 fields 필터는 현재 _STATE_KEYS 전체에서 자유 선택.
  클라이언트가 임의 키를 요청해도 collect_state() 에 없으면 결과에 안 잡힘 → 무해.
  다만 클라이언트별로 허용 필드를 제한하고 싶다면 레지스트리 확장 필요.
구현 방향 (필요 시):
  .env.webhook 에 CLIENT_N_ALLOWED_FIELDS=flow_score,entry_signal,bias 형식 추가
  find_by_token() 반환 클라이언트에 allowed_fields 필드 추가
  routes_webhook.py 에서 교집합 필터 적용
전제조건: 클라이언트별 접근 제어 정책이 실제로 필요해진 시점

================================================================================

[2026-03-23] ★★★ 5분 전략 신호 엔진 자동 보정 시스템 구현

  개요:
    5분 전략 방향 엔진(PulseEngine)의 지표별 가중치와 임계값은
    처음부터 고정된 정답값으로 단정하지 말고,
    로그 기반 검증을 통해 점진적으로 보정하는 방식으로 운영한다.

  설계 원칙:
    ① 지표 역할 분리
       - 방향 지표      : 외국인 선물 delta, flow_score, 외국인 현물 delta, long/short 점수차
       - 추세 강화 지표  : OI 변화, EMA 기울기, 베이시스 기울기
       - 실행 제한 지표  : 횡보(RANGE), CHOPPY, EXHAUSTION, 외국인 delta 둔화
       → 모든 지표를 단일 합산하지 말고 역할별 레이어로 분리

    ② 등급화된 초기 가중치
       - 처음부터 정밀 수치(0.37, 0.22...)로 시작하지 말 것
       - 강/약/중립 등 3~5단계 구간화로 시작
       - 예: 강한 매도=-2, 약한 매도=-1, 중립=0, 약한 매수=+1, 강한 매수=+2

    ③ 2단계 점수 구조
       - 1단계: 방향 점수 (BULLISH/NEUTRAL/BEARISH 후보 산출)
       - 2단계: 신뢰 보정 점수 (후보 방향의 신뢰도 확정/약화)
       → 방향과 신뢰를 분리해야 오판 시 추적 가능

  로그 수집 (매 5분 판정 시점):
    - 입력 지표 원본값 전체
    - 각 지표별 계산된 점수
    - 발동한 규칙 목록
    - 최종 방향 판정 (BULLISH / NEUTRAL / BEARISH)
    - 신뢰도
    - 후보/확정 상태

  자동 채점 (판정 후 자동):
    - 5분 후 실제 시장 결과 확인 → 성공/실패 기록
    - 10분 후 확인
    - 15분 후 확인
    → 각 시점별 성공률 누적

  보정 절차:
    - 실패 사례를 유형별 분류
        * 과민반응: 너무 자주 방향 전환
        * 둔한 전환: 추세 전환을 늦게 잡음
        * 횡보 오판: 횡보를 추세로 잘못 판단
    - 전체 규칙을 한꺼번에 바꾸지 말 것
    - 가장 문제가 큰 임계값 또는 특정 지표 가중치 한 항목만 소폭 수정
    - 수정 후 다시 로그 수집 → 반복

  ★★★ 최종 목표 — 자동 최적화 코드 구현:
    프로그램이 누적 로그를 바탕으로 아래 항목을 자동 산출한다:
      - 지표별 기여도 분석
      - 임계값 후보 제안
      - 방향 판정 정확도 (5분/10분/15분 기준)
      - 평균 방향 유지 시간
      - flip(방향 전환) 횟수
      - 횡보 오판율
    → 산출된 최적 가중치와 임계값을 기반으로
       5분 전략 신호를 자동 판별하는 엔진 코드를 작성한다.
    → 수동 감각이 아닌 데이터 기반 자기보정 엔진으로 진화하는 것이 최종 목표.

  구현 순서 (참고):
    1. PulseEngine v1: 역할 분리 + 등급화 규칙으로 1차 설계
    2. PulseLogger: 매 판정 시점 전체 상태 DB 기록
    3. PulseScorer: 5/10/15분 후 자동 채점
    4. PulseAnalyzer: 누적 로그 분석, 실패 유형 분류
    5. PulseCalibrator: 최적 가중치/임계값 자동 산출
    6. PulseEngine v2: 자동 산출값 반영 자기보정 엔진

  관련 보고서:
    - docs/history.txt [2026-03-23] 5분 전략 신호 엔진 설계 보고서 v2.0

[2026-03-23] ★★ PulseValidator 강한 불일치(strong_miss) 처리 단계형 재검토

  현재 설계:
    - 1회 강한 불일치 → 진입 금지 (포지션 유지)
    - 2회 연속 강한 불일치 → 포지션 청산 권고 + 강제 NEUTRAL 알림

  우려 사항:
    - Validator는 30분 인터벌 LLM(Haiku) 단 한 번의 판단으로 메인 엔진을 차단함
    - LLM은 모멘텀 전환점에서 과반응하는 경향이 있어 오탐률 높아질 수 있음
    - 실전 운영 중 "강한 불일치" 판정이 빈번하면 validator가 엔진을 과하게 흔들 수 있음
    - 강한 불일치 판정 기준(strong_miss)이 충분히 검증되지 않은 상태임

  재검토 후보 방향 (데이터 확보 후 결정):
    - 단계형 대응으로 전환:
        1단계: 포지션 사이즈 절반 축소
        2단계: 손절선 타이트하게 조정
        3단계: 추가 악화 시(3회 연속?) 전량 청산
    - 현재 "2회 연속 → 청산 권고" 임계값 상향 검토 (2회 → 3회)

  재검토 시점:
    - 2~3주 실전 운영 후 validator 로그에서 strong_miss 발생 빈도 확인
    - 오탐(엔진이 맞고 LLM이 틀린 케이스) 비율이 30% 초과 시 단계형 전환
    - 오탐 비율이 낮으면 현행 유지

  주의: 지금 당장 수정 금지
    - 단계형 로직은 파라미터 3개 이상 추가됨
    - 데이터 없이 파라미터 설정 시 설계 임의성 증가
    - 운영 로그 없이는 어떤 기준도 근거 없음

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🟡 시장 읽기 데이터 보완 (2026-03-23 확인)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[D-001] 당일 레인지 내 현재가 위치 (%) 실시간 추적
────────────────────────────────────────────────────────────────────────────────
근거:
  2026-03-23 장 분석에서 "종가가 레인지 하단 11.3% 위치" 가
  약세 확신도 산출 시 두 번째로 높은 가중치(20%) 항목으로 확인됨.
  MarketAnalyst는 3분 단위 high_pos_pct/low_pos_pct를 계산하나,
  당일 전체 레인지(일봉) 기준 현재가 위치는 미수집.

추가 내용:
  - 당일 고가(session_high) / 저가(session_low) 누적 추적
  - 현재가의 레인지 내 위치 = (price - session_low) / (session_high - session_low)
  - state 키: session_high, session_low, price_range_pct
  - 활용: CheckpointAnalyst / MarketAnalyst 컨텍스트 보강, PulseEngine 보조 지표

시점: 데이터 설계 검토 후 구현 (1~2주)
우선순위: 🟡 중간

[D-002] 기관 방어 실패 신호 (Institution Defense Failure)
────────────────────────────────────────────────────────────────────────────────
근거:
  2026-03-23: 기관합 +18,039계약 순매수에도 불구하고 -6.79% 하락 마감.
  "기관이 받아냈지만 방향을 못 돌린 날" = 추세 강도 가장 높은 신호 중 하나.
  현재 MMEAN은 기관 순매수 방향만 보고, 가격 방향과의 역행 여부를 미체크.

추가 내용:
  - inst_defense_fail = (fut_inst_delta > 임계값) AND (price_change < -임계값)
  - 조건 충족 시 state["inst_defense_fail"] = True
  - PulseEngine 스코어 보조 지표로 활용 (약세 추가 가중)
  - 임계값: 기관 +5,000계약 이상 매수 AND 가격 -1.5% 이상 하락

시점: fut_inst_delta 2주 데이터 수집 후 임계값 설정
우선순위: 🟡 중간
주의: 임계값은 데이터 확인 전 고정 금지


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🟡 2주 운영 계획 (2026-03-23 기준)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[P-001] pulse_sim.bat 무한 루프 — PulseEngine 임계값 탐색
────────────────────────────────────────────────────────────────────────────────
실행: pulse_sim.bat (인자 없이 실행 → 자동 무한 루프)
대상: sim_opt/pulse_sim.py
결과: storage/pulse_sim.db / pulse_runs 테이블

동작 흐름:
  1. mmean.db / regime_ticks 에서 최근 정규장 날짜 자동 탐지
  2. 마지막 study 날짜와 비교 → 새 날짜 생기면 자동 재실행
  3. 랜덤 탐색 300회 → Bayesian(Optuna TPE) 150회 순서로 실행
  4. 새 날짜 없으면 300초 대기 후 재확인

탐색 대상 파라미터 (pulse_engine.py 상수 대응):
  a_bullish_thresh      A-group BULLISH 판정 임계값        현재 +4.0
  fgn_delta_strong      외국인 delta 강도 기준 (계약수)     현재 1000
  flow_upper / lower    flow_score 분기점                  현재 0.5 / 0.1
  ls_diff_thresh        long-short 차이 기준               현재 0.2
  ema_slope_thresh      EMA 기울기 유의미 기준              현재 0.05
  basis_high            베이시스 고/저 기준                 현재 0.3
  b_confirm_min         B-group confirmed 최소값            현재 1
  trend_confirm_count   추세 확정 연속 횟수                 현재 3
  confidence_min_confirm confirmed 불가 최소 신뢰도         현재 0.5
  confidence_min_entry  진입 허용 최소 신뢰도              현재 0.3

목적함수 (PnL 기반 아님 — 신호 품질 기반):
  direction_acc × 100         5분 후 방향 적중률
  + confirmed_ratio × 20      confirmed 신호 비율
  - neutral_penalty × 30      A-score 강한데 NEUTRAL 오판 패널티
  + entry_count / rate 보정    진입 신호 과소/과다 패널티

참고 파일: docs/pulse_sim_options.txt

2주 후 작업:
  - pulse_sim.db 상위 10~20건 결과 검토
  - direction_acc ≥ 0.8 AND entry_count ≥ 5 인 파라미터 세트 선별
  - pulse_engine.py 상수값 수동 업데이트
  - 이후 가중치(A1 ×2, A2 ±2 등 배점 자체) 탐색 범위 추가 예정

────────────────────────────────────────────────────────────────────────────────

[P-002] sim.bat 자동 루프 — 진입/청산 파라미터 탐색
────────────────────────────────────────────────────────────────────────────────
실행: sim.bat → [a] 자동 루프 설정 → 새 창에서 실행
대상: scripts/day_auto_run.py → sim_opt/day_sim.py
결과: storage/sim.db / sim_runs, sim_trades 테이블

동작 흐름:
  1. regime_ticks 에서 정규장 날짜 자동 탐지
  2. 랜덤 탐색 (study: day_auto_XXX) → Bayesian 최적화 순서로 실행
  3. 루프마다 study 번호 자동 채번

탐색 대상 파라미터:
  enter_score           진입 점수 기준
  enter_gap             long/short 점수 차 기준
  min_confidence        최소 신뢰도
  flow_score_gate_thr   flow_score 진입 필터
  sim_sl_ticks          손절 틱수
  sim_tp_ticks          목표 틱수
  sim_trailing_*        트레일링 스탑
  range_pos_gate        레인지 위치 필터
  fut_fgn_dir_required  외국인 방향 일치 필터
  fgn_sync_required     현물-선물 외국인 동조 필터
  (외 다수)

현황 (2026-03-23):
  03-18: day_auto_* study 랜덤+베이지안 완료
  03-19: auto_* study 랜덤+베이지안 완료
  03-20: 랜덤+베이지안 완료
  03-23: 가상 트레이딩 진행 중 → 장 종료 후 자동 루프가 처리

2주 후 작업:
  - sim.bat [6] Validation → train 날짜 지정, valid 날짜 = 2주 후 날짜로 지정
  - train/valid 날짜가 반드시 다른 기간이어야 유효
  - validation 통과 시 sim_adoptions → 실 트레이딩 파라미터 자동 적용

────────────────────────────────────────────────────────────────────────────────

[P-003] 2주 후 가중치 탐색 확장 (pulse_sim.py)
────────────────────────────────────────────────────────────────────────────────
현재: 임계값만 탐색 (a_bullish_thresh, b_confirm_min 등)
예정: A/B 그룹 배점 자체도 탐색 범위에 추가

추가 예정 파라미터:
  a1_weight     A1(외국인 delta) 가중치     현재 ×2 고정 (최대 ±4)
  a2_max        A2 최대 배점               현재 ±2 고정
  a3_max        A3 최대 배점               현재 ±2 고정
  a4_max        A4 최대 배점               현재 ±1 고정
  b1_max        B1 최대 배점               현재 ±2 고정
  b2_max        B2 최대 배점               현재 ±1 고정
  b3_max        B3 최대 배점               현재 ±1 고정

전제조건:
  - 2주 이상 데이터 축적으로 통계적 의미 확보
  - 임계값 탐색 결과로 기본 방향 확인 후 진행
  - 파라미터 수 증가로 탐색 공간 확대 → random/bayes trial 수 증가 필요

우선순위: 🔵 낮음 (2주 후 검토)

────────────────────────────────────────────────────────────────────────────────

[P-004] pulse_sim.db 밸리데이션 — 패턴별 파라미터 클러스터링  [예정 2026-03-28]
────────────────────────────────────────────────────────────────────────────────
배경:
  일별 실제 패턴(TREND_UP/DOWN, CHOPPY, MORNING_SPIKE 등)을 정답 레이블로 사용.
  pulse_sim 상위 결과를 날짜별 패턴으로 그룹화 → 패턴별 최적 파라미터 추출.
  매주 금요일 마감 후 실행.

1차 밸리데이션 대상 날짜 및 패턴:
  03-16 TREND_UP / 03-17 CHOPPY / 03-18 TREND_UP
  03-19 CHOPPY / 03-20 TREND_DOWN / 03-23 TREND_DOWN

작업 순서:
  1. pulse_sim.db 날짜별 상위 파라미터 추출
  2. 패턴별 클러스터 비교 (TREND vs CHOPPY 파라미터 차이)
  3. 교차 검증 (같은 패턴 날짜 간 파라미터 일반화 성능)
  4. 현재 운영 파라미터와 비교 → 수동 반영 여부 결정

우선순위: 🔴 높음 (2026-03-28 실행 예정)

────────────────────────────────────────────────────────────────────────────────

[P-005] DB 분리 — judgment.db, pulse.db 독립  [장기]
────────────────────────────────────────────────────────────────────────────────
배경:
  현재 mmean.db에 모든 테이블 혼재 → WAL lock 경합 빈도 높음.
  분리 대상:
    judgment_events → judgment.db
    pulse_history, pulse_signals → pulse.db
  효과: 각 DB의 write 경합 감소, busy_timeout 오류 감소
우선순위: 🔵 낮음 (현재 busy_timeout 2초로 완화 상태이므로 긴급하지 않음)

────────────────────────────────────────────────────────────────────────────────

[P-006] MarketAnalyst 이벤트 트리거 — 임계치 튜닝  [DONE 기반구조 2026-03-25]
────────────────────────────────────────────────────────────────────────────────
배경:
  2026-03-25 이벤트 트리거 방식으로 전환 완료.
  현재 임계치는 초기값으로 실전 데이터 기반 튜닝 필요.

현재 임계치 (engines/market_analyst.py 상단):
  COOLDOWN_SEC     = 120   # 연속 호출 방지 최소 간격
  EMA_SLOPE_FLIP   = 0.15  # EMA 기울기 부호 전환 감지 최소 크기
  EMA_SLOPE_ACCEL  = 0.8   # EMA 기울기 급가속 임계치
  VOL_BURST_THRESH = 2.2   # 거래량 폭발 임계치
  FLOW_SIGN_THRESH = 0.06  # flow_score 부호 전환 감지 최소 크기

조정 방향:
  - 1주일 운영 후 트리거 빈도 확인 (state["market_view_trigger"] 이력 참조)
  - 하루 5~15회가 적정 (너무 많으면 COOLDOWN_SEC 증가, 너무 적으면 임계치 완화)
  - EMA_SLOPE_FLIP: 장 초반 노이즈 많으면 0.2~0.25로 높이기
  - COOLDOWN_SEC: 추세 전환 빠른 날은 60초로 줄이는 것도 검토

우선순위: 🟡 중간 (1주일 후 점검)

────────────────────────────────────────────────────────────────────────────────

[P-007] sim.db TREND_DOWN 날짜 데이터 보강  [예정]
────────────────────────────────────────────────────────────────────────────────
배경:
  03-20, 03-23 (TREND_DOWN) run수 각 1,648 / 2건으로 극히 부족.
  03-18 TREND_UP 118,177건 대비 심각한 패턴 불균형 → TREND_DOWN 파라미터 미최적화.

조치:
  TREND_DOWN 날짜 집중 탐색: study.bat 260320 또는 study.bat 260323 실행
  → 각 패턴당 최소 10,000건 이상 확보 후 밸리데이션 진행

우선순위: 🔴 높음 (밸리데이션 전에 해결)

────────────────────────────────────────────────────────────────────────────────

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🟡 LLM 역할 확장 로드맵 (2026-03-26 논의)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[A-010] LLM3분 유효성 검증 — market_view_log × sim_trades 교차 분석
────────────────────────────────────────────────────────────────────────────────
배경:
  MarketAnalyst가 출력한 view(BULLISH/BEARISH/NEUTRAL)가 실제 가격 방향과
  얼마나 일치하는지 데이터로 검증 전까지 LLM 청산 권한 확장은 보류.
  intraday.db market_view_log에 view 이력이 쌓이는 중.

분석 항목:
  - view 정확도: view 출력 후 3분/5분/10분 내 실제 가격 방향 일치율
  - 오트리거 비율: LLM 호출 후 신호 없이 끝난 비율
  - 청산 유효성: REVERSAL 또는 FLAT_HIGH view 시 포지션이 실제 손실로 이어졌는지
  - 허위 경보: BEARISH view 후 가격이 오른 케이스 비율

분석 방법:
  market_view_log (session_date, ts, view, direction) × sim_trades (open_ts, close_ts, pnl)
  → 겹치는 구간에서 view와 실제 손익 방향 비교

시점: 1~2주 데이터 축적 후 (2026-04월 초)
우선순위: 🟡 중간 (청산 확장의 전제 조건)

[A-011] MarketAnalyst view → 청산 플래그 배선 (데이터 검증 후)
────────────────────────────────────────────────────────────────────────────────
배경:
  현재 MarketAnalyst view는 state["market_view"]에 저장만 되고
  엔진의 청산 로직과 연결되지 않음 (데이터 검증 없이 배선하면 오탐 위험).

구현 방향 (A-010 검증 통과 후):
  - view = "REVERSAL_DOWN"  → LONG 포지션 청산 플래그 활성
  - view = "FLAT_HIGH"      → LONG 포지션 청산 플래그 활성 (레인지 상단 횡보)
  - view = "REVERSAL_UP"    → SHORT 포지션 청산 플래그 활성
  - 플래그: state["llm_exit_signal"] = {"direction": "LONG", "reason": "..."}
  - SimEngine / LiveExecutor에서 T6 트리거로 처리

전제조건: A-010 검증 완료 (view 정확도 ≥ 65%, 허위 경보 ≤ 30%)
우선순위: 🔵 낮음 (검증 전 구현 금지)

[A-012] LLM exit 권한 구현 — FORCE_EXIT_LLM 트리거 추가
────────────────────────────────────────────────────────────────────────────────
배경:
  현재 LLM은 진입 전 weight_adjust만 출력 가능. 보유 중 청산 권한 없음.
  A-011 배선 완료 후 엔진에 T6 FORCE_EXIT_LLM 트리거를 추가한다.

역할 분담 원칙 (2026-03-26 확정):
  진입 결정      → 엔진 전담 (속도)
  손절 실행      → 엔진 전담 (속도)
  익절 실행      → 엔진 전담
  청산 판단 지원 → 엔진 + LLM (LLM은 "나와라" 신호만, "버텨라"는 신호 없음)
  파라미터 폭    → LLM (sl_ticks, trailing_ticks, enter_score 범위 제안)
  큰 흐름 판단   → LLM 10분 (MarketAnalyst)

비대칭 권한 원칙:
  LLM은 포지션을 늘리는 방향보다 줄이고 나오는 방향에 더 큰 권한을 가진다.
  WA_MAX_UP = +0.10 (진입 확대 제한)
  WA_MAX_DOWN = -0.20 (청산 방향 확대 허용)
  → 이 비대칭은 청산 트리거 확장 시에도 동일하게 적용한다.

구현 전제조건:
  1. A-010 유효성 검증 완료
  2. A-011 view 배선 및 1주 shadow 운영 확인
  3. POLICY.md FORCE_EXIT_LLM 섹션 추가

우선순위: 🔵 낮음 (2026-04월 이후)

[A-013] LLMPulse 외국인 순매수 — 총량 누적값 → 델타값 전환  [DONE 2026-03-26]
────────────────────────────────────────────────────────────────────────────────
발견일: 2026-03-26
증상:
  오늘 장중(12:46 기준) BULLISH 판정 0건. 차트상 약 18분간 BULLISH 구간이
  명확히 존재했음에도 불구하고 엔진이 전혀 감지하지 못함.

원인 분석:
  _summarize() 내 foreign_buy 계산이 현재 두 가지 방식이 혼재:
  - ctx_snap.foreign_buy → state에서 읽는 누적 순매수 총량 (오전부터 합산)
  - tick summary.foreign_buy → 5분 delta (last-first, 이미 델타 계산됨)

  문제는 _call_haiku()에 넘기는 user_prompt에서
  ctx['foreign_buy']를 그대로 노출하는 부분:
    "- 외국인 5분 순매수 변화: {summary['foreign_buy']:+,} (누적 아님·5분 delta)"
  와 별개로 엔진 상태 컨텍스트의 foreign_buy는 누적 총량이기 때문에,
  오전부터 순매도가 쌓인 날에는 오후 단기 순매수 반전이 있어도
  총량이 음수(-)여서 LLM이 BULLISH 조건을 충족하기 어렵다.

  BULLISH 조건 중 하나:
    "외국인 순매수 양수이고 가격이 VWAP 위"
  → 총량 기준이면 오전 대량 순매도 후 오후 회복 구간에서 조건 미충족.

개선 방향:
  1. ctx_snap.foreign_buy를 5분 delta로 전환
     (현재 tick summary.foreign_buy와 동일한 계산 방식: last-first of recent ticks)
  2. 또는 LLM 프롬프트의 BULLISH 조건 재작성:
     "외국인 5분 delta 양수이고 가격이 VWAP 위" 로 변경
  3. 누적 총량은 별도 참고 변수로 유지하되 조건 판단에서는 제외

  차트 근거:
  - 2026-03-26, 11:33 저점(809.35) → 12:42 820.45 반등
  - BULLISH 구간 약 18분 존재, 대시보드 BULLISH=0 / NEUTRAL=88 / BEARISH=12

전제조건: 없음 (즉시 수정 가능)
우선순위: 🟡 중간 (LLM Pulse 정확도 직결)

================================================================================
[A-031] 진입 타이밍 모듈 신규 (EntryTiming) — 레짐/LLM과 분리
================================================================================
우선순위: 중 | 처리 가능 시점: RAG 데이터 1~2주 축적 후
배경:
  - 레짐 엔진(EMA 기반)과 LLM은 구조적으로 5~8분 후행
  - 레짐 확정 = 진입인 현재 구조에서 휩쏘 및 늦은 진입 불가피
  - 2026-03-26: 하락 23분 시작, 엔진 35분 감지, LLMCaller 30분 청산신호
핵심 설계:
  - 레짐(방향 판단)과 진입타이밍(실행 시점)을 완전 분리
  - 레짐: "무엇을 할지" — 늦어도 됨 (방향 정확성이 목적)
  - 진입: "언제 할지" — 빠른 마이크로 신호 사용
마이크로 선행 신호 후보 (EMA보다 1~2분 빠름):
  - volume_burst 급등 (거래량 폭발)
  - basis 급변 (선/현물 스프레드 역전)
  - price vs VWAP 이격 확대
  - oi_delta (미결제약정 급변)
  - price_velocity (틱당 가격 속도)
진입 조건 예시: 레짐 방향 확정 AND 마이크로 신호 3개 중 2개 충족 → 진입
구현 방향:
  - EntryTimingEngine 신규 클래스 (engines/entry_timing.py)
  - 레짐 방향을 입력받아 마이크로 신호로 진입 타이밍 결정
  - sim_trades와 대조하여 signal → 실제 수익 검증

================================================================================
[A-032] 진입 전용 RAG 학습 시스템
================================================================================
우선순위: 낮 | 처리 가능 시점: sim_trades 1~2주 + EntryTiming 구현 후
배경:
  - 기존 RAG: 패턴 → sl/trailing/enter_score 파라미터 추천
  - 진입 RAG: 진입 직전 30~60초 마이크로 스냅샷 → 진입 가능성/위험도
학습 데이터:
  - 입력: 진입 시점 직전 스냅샷
          (volume_burst, basis_delta, vwap_dist, oi_delta, price_velocity)
  - 레이블: 이후 실제 수익/손실 (sim_trades 체결 결과)
출력:
  - enter_confidence: 진입 적합도 (0~1)
  - whipsaw_risk: 휩쏘 위험도 (0~1)
  - suggested_wait_ticks: 진입 전 대기 권장 틱 수
관련: A-031 EntryTiming 구현 이후 단계
